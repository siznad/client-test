{
  "name": "client-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "common-tags": "^1.8.0",
    "twilio": "^3.47.0"
  },
  "devDependencies": {
    "twilio-run": "^2.7.0"
  },
  "engines": {
    "node": "10"
  }
}
